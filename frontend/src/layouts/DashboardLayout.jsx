import { Link, NavLink, Outlet, useLocation } from 'react-router-dom';import { useDispatch, useSelector } from 'react-redux';import { logout } from '../features/auth/authSlice.js';import { Avatar } from '../components/ui/Avatar.jsx';import { ThemeToggle } from '../components/ThemeToggle.jsx';
export default function DashboardLayout(){const {user}=useSelector(s=>s.auth);const dispatch=useDispatch();const crumbs=useLocation().pathname.split('/').filter(Boolean);const nav=[['/dashboard','Dashboard'],['/dashboard/profile','Profile']];return <div className="min-h-screen bg-slate-50 text-slate-950 dark:bg-slate-950 dark:text-white md:flex"><aside className="border-b bg-white p-4 dark:border-slate-800 dark:bg-slate-900 md:min-h-screen md:w-64 md:border-r"><Link to="/dashboard" className="text-xl font-black">QueueIt</Link><nav className="mt-8 grid gap-2">{nav.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`rounded-xl px-3 py-2 text-sm font-medium ${isActive?'bg-slate-950 text-white dark:bg-white dark:text-slate-950':'hover:bg-slate-100 dark:hover:bg-slate-800'}`}>{label}</NavLink>)}</nav>{user?.role&&<p className="mt-6 text-xs uppercase tracking-wide text-slate-500">Role: {user.role}</p>}</aside><section className="flex-1"><header className="sticky top-0 z-10 flex items-center justify-between border-b bg-white/80 px-6 py-4 backdrop-blur dark:border-slate-800 dark:bg-slate-950/80"><div><p className="text-xs text-slate-500">/{crumbs.join(' / ')}</p><h1 className="font-semibold">Workspace</h1></div><div className="flex items-center gap-3"><ThemeToggle/><Avatar name={user?.name||user?.email}/><button onClick={()=>dispatch(logout())} className="text-sm font-semibold">Logout</button></div></header><main className="p-6"><Outlet/></main></section></div>}
