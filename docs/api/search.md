# Search

Search endpoints use `GET /api/v1/{resource}/search?q=term&limit=25`. Search is case-insensitive for approved text fields such as names, email, phone suffixes, token numbers, and audit action labels. Tenant scoping still applies.
