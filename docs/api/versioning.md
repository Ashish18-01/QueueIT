# Versioning

The API uses URI versioning: `/api/v1`. Additive fields and endpoints are backward compatible. Breaking changes require `/api/v2`, at least 180 days of deprecation notice, migration examples, and response headers `Deprecation`, `Sunset`, and `Link` where applicable.
